# API Keys (for AI usage only)
# Only Gemini is available right now and uses Gemini 2.5 Flash Lite (you can change the model by modifying the code at app.py:383)
GEMINI_API_KEY = ""

# Databases Identifiers
## Redis (Redis is used for real-time data during active games)
REDIS_URL = ""